/*package com.cm.main;

import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.cm.flashmob.service.FlashmobService;
import com.cm.flashmob.vo.Flashmob;

@WebServlet("")
public class PopularBoardServlet extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public PopularBoardServlet() {
        super();
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {*/
		/*
		 * List<Flashmob> list = new FlashmobService().popularFlashmob(); if (list !=
		 * null) { request.setAttribute("resultList", list);
		 * System.out.println(list.get(0).getPost_title()); // 로그 확인용 } else {
		 * request.setAttribute("resultList", new ArrayList<Flashmob>()); // 빈 리스트 전달 }
		 * 
		 * RequestDispatcher view = request.getRequestDispatcher("/index.jsp");
		 * view.forward(request, response);
		 */
		/*
		 * }
		 * 
		 * protected void doPost(HttpServletRequest request, HttpServletResponse
		 * response) throws ServletException, IOException { doGet(request, response); }
		 * 
		 * }
		 */
